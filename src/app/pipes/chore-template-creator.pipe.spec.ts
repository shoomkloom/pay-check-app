import { ChoreTemplateCreatorPipe } from './chore-template-creator.pipe';

describe('ChoreTemplateCreatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ChoreTemplateCreatorPipe();
    expect(pipe).toBeTruthy();
  });
});
